//We’ll define here the mutations and the associated resolvers to each mutation type

const Mutation = '';
// `
//   extend type Mutation {
//   }
// `;

export const mutationTypes = () => [Mutation];

export const mutationResolvers = {

};